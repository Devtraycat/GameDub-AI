# Capture
mss>=9.0.1
pygetwindow>=0.0.9      # sadece "window" modu için (Windows'ta en stabil)

# OCR
rapidocr-onnxruntime>=1.3.0
# Türkçe altyazı yakalama (ocr.lang="tr") için: pytesseract>=0.3.10, opencv-python>=4.9.0
# + sistemde tesseract-ocr VE "tur" dil paketi kurulu olmalı:
#   Linux:   sudo apt-get install tesseract-ocr tesseract-ocr-tur
#   Windows: Tesseract kurulumunda "Turkish" dil paketini işaretleyin

# Çeviri (varsayılan zincir: argos -> deepl -> google)
argostranslate>=1.9.6
deepl>=1.18.0           # opsiyonel, DEEPL_API_KEY gerekir
deep-translator>=1.11.4 # opsiyonel, google fallback için

# TTS (v1.7 varsayılan zincir: piper -> edge_tts -> pyttsx3)
# piper: yerel, küçük (~60MB), ONNX tabanlı, HIZLI Türkçe TTS (birincil motor).
#        Model dosyaları ilk çalıştırmada otomatik indirilir (models/piper/).
piper-tts>=1.2.0
edge-tts>=6.1.0
pyttsx3>=2.90

# Ses çalma (PortAudio tabanlı, prebuilt wheel'lar, derleyici gerekmez)
sounddevice>=0.4.6
soundfile>=0.12.1   # WAV ve MP3 (edge-tts çıktısı) formatlarını aynı arayüzle çözer

numpy>=1.26.0
